1. 	Вывод различных атрибутов процесса в соответствии с указанными опциями
Напишите программу, которая будет обрабатывать опции, приведенные ниже. Опции должны быть обработаны в соответствии с порядком своего появления справа налево. Одной и той же опции разрешено появляться несколько раз. Используйте getopt(3C) для определения имеющихся опций. Сначала пусть ваша программа обрабатывает только некоторые опции. Затем добавьте еще, до тех пор, пока все требуемые опции не будут обрабатываться. Вы можете скопировать воспользоваться программой getopt_ex.c и изменить ее.
•	-i  Печатает реальные и эффективные идентификаторы пользователя и группы.
•	-s  Процесс становится лидером группы. Подсказка: смотри setpgid(2).
•	-p  Печатает идентификаторы процесса, процесса-родителя и группы процессов.
•	-u  Печатает значение ulimit
•	-Unew_ulimit  Изменяет значение ulimit. Подсказка: смотри atol(3C) на странице руководства strtol(3C)
•	-c  Печатает размер в байтах core-файла, который может быть создан.
•	-Csize  Изменяет размер core-файла
•	-d  Печатает текущую рабочую директорию
•	-v  Распечатывает переменные среды и их значения
•	-Vname=value  Вносит новую переменную в среду или изменяет значение существующей переменной.
 
Проверьте вашу программу на различных списках аргументов, в том числе:
•	Нет аргументов
•	Недопустимую опцию.
•	Опции, разделенные знаком минус.
•	Неудачное значение для U.


______________________________________________________
______________________________________________________
Последовательность решения
PS C:\Users\1\Desktop\Операционные системы\Задание_2> ssh i.kozulin@ccfit.nsu.ru    
i.kozulin@ccfit.nsu.ru's password: 
Last login: Wed Sep  9 16:09:44 2026 from 172.16.75.27
i.kozulin@mephisto:~$ ssh solaris
The Illumos Project     SunOS 5.11      illumos-8b06eaf6ac      February 2020
i.kozulin@fit-main:~$ ls
MyOSTest    gitnsu.pub
i.kozulin@fit-main:~$ cd MyOSTest/

______________________________________________________
# Ключевые концепции курса (Unix SVR4 / Solaris) с привязкой к задаче

## 1. Архитектура и Процесс
• Userland vs Kernel:
  - Userland: пользовательский код (наша программа options.c)
  - Kernel: ядро ОС, обрабатывает системные вызовы (getpid, setpgid)
• Процесс — изолированная среда исполнения:
  - Имеет уникальный PID (выводится опцией -p)
  - Владеет ресурсами: UID/GID (опция -i), ulimit (опция -u/-U)

## 2. Иерархия процессов
• init (PID 1) → shell (bash) → ./options
• getppid() возвращает PID shell'а (родителя)
• setpgid(0, 0) (опция -s) делает процесс лидером новой группы (PGID = PID)

## 3. Атрибуты процесса (системные вызовы man 2)
• Идентификаторы:
  - getuid()/geteuid() — реальный/эффективный UID пользователя
  - getgid()/getegid() — реальный/эффективный GID группы
• Ресурсы:
  - getrlimit(RLIMIT_NOFILE) — текущий ulimit (лимит открытых файлов)
  - getrlimit(RLIMIT_CORE) — лимит размера core-дампа
  - setrlimit() — изменение этих лимитов
• Файловая система:
  - getcwd() — получение пути текущей директории (опция -d)

## 4. Переменные среды (библиотека C man 3C)
• environ — глобальный массив строк "KEY=VALUE"
• putenv("NAME=val") — добавление/изменение переменной (опция -V)
• Цикл по environ выводит все переменные (опция -v)

## 5. Парсинг аргументов (getopt)
• getopt(argc, argv, "ispuU:cC:dvV:")
  - i,s,p,u,d,v — флаги без аргументов
  - U:, C:, V: — опции с обязательным аргументом
• optarg — указатель на значение аргумента (например, "1024" для -U)
• optopt — символ ошибочной опции (при возврате '?')

## 6. Основные библиотеки

#include <stdlib.h>       // realloc, free, exit, strtol
#include <stdio.h>        // printf, fprintf
#include <unistd.h>       // getopt, getpid, getuid, getcwd
#include <sys/resource.h> // struct rlimit, getrlimit, setrlimit
#include <limits.h>       // PATH_MAX
#include <string.h>       // strdup, strchr
#include <errno.h>        // errno
extern char **environ;    // Глобальный указатель на массив переменных среды

________________________________________________________________
# Создаем директорию для первого задания
mkdir task_1

# Переходим в нее
cd task_1

# Проверяем, что мы в нужной директории
pwd

Теперь создадим файл options.c с кодом и создаем файл Makefile 
nano options.c
# ctrl+O, затем ctrl+X
nano Makefile
# ctrl+O, затем ctrl+X

i.kozulin@fit-main:~/MyOSTest/task_1$ ls
Makefile   options.c

________________________________________________________________

1.Базовая структура программы
Сначала создадим пустую программу, которая просто принимает аргументы:

#include <stdio.h>
#include <stdlib.h>

int main(int argc, char *argv[]) {
    // Print number of arguments
    printf("Number of arguments: %d\n", argc);
    
    // Print all arguments
    for (int i = 0; i < argc; i++) {
        printf("argv[%d] = %s\n", i, argv[i]);
    }
    
    return 0;
}

Создаём файл options.c с кодом
Скомпилируем: gcc -o options options.c
Запустим: ./options -i -p -d
Посмотри вывод
# gcc — компилятор C (GNU Compiler Collection)
# -O2 — уровень оптимизации 2 (баланс скорости/размера бинарника)
# -o options — имя выходного исполняемого файла
# options.c — исходный код на C

# Nano: выделение и удаление текста

# Выделение:
Alt + A          # начать выделение (Set Mark)
Стрелки          # расширить выделение

# Удаление выделенного:
Ctrl + K         # вырезать (Cut)

# Альтернатива (без выделения):
Ctrl + K         # удалить текущую строку
Ctrl + K × N     # удалить N строк подряд

# Вставка вырезанного:
Ctrl + U         # вставить (Uncut)

# Отмена действия:
Alt + U          # undo
Alt + E          # redo

# Nano: основные команды

# Навигация:
Ctrl + F         # вперёд на символ
Ctrl + B         # назад на символ
Ctrl + P         # строка вверх
Ctrl + N         # строка вниз
Ctrl + A         # начало строки
Ctrl + E         # конец строки
Ctrl + V         # страница вниз
Ctrl + Y         # страница вверх
Ctrl + _         # перейти к строке/столбцу

# Редактирование:
Ctrl + K         # вырезать строку
Ctrl + U         # вставить
Ctrl + Space     # автодополнение
Alt + U          # отменить
Alt + E          # повторить

# Выделение:
Alt + A          # начать/завершить выделение
Ctrl + ^         # копировать выделенное (Copy)
Ctrl + K         # вырезать выделенное (Cut)

# Поиск:
Ctrl + W         # найти текст
Alt + W          # повторить поиск
Ctrl + \         # найти и заменить

# Файл:
Ctrl + O         # сохранить (Write Out)
Ctrl + R         # вставить файл
Ctrl + X         # выход (с запросом сохранения)

# Помощь:
Ctrl + G         # справка

!!!!! Если комбинация не работает в Vs Code
File -> Preferences -> Settings
Пишем Terminal allow chords
Убираем галки в терминале и workspace

________________________________________________________________
Подключаем getopt()

#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>

int main(int argc, char *argv[]) {
    // All options: s, p, u, d don't need arguments
    char *options = "ispuU:cC:dvV:";
    int c;
    
    // Parse options
    while ((c = getopt(argc, argv, options)) != -1) {
        switch (c) {
            case 'i':
                printf("Option -i detected\n");
                break;
            case 's':
                printf("Option -s detected\n");
                break;
            case 'p':
                printf("Option -p detected\n");
                break;
            case 'u':
                printf("Option -u detected\n");
                break;
            case 'U':
                printf("Option -U with value: %s\n", optarg);
                break;
            case 'c':
                printf("Option -c detected\n");
                break;
            case 'C':
                printf("Option -C with value: %s\n", optarg);
                break;
            case 'd':
                printf("Option -d detected\n");
                break;
            case 'v':
                printf("Option -v detected\n");
                break;
            case 'V':
                printf("Option -V with value: %s\n", optarg);
                break;
            case '?':
                printf("Invalid option: %c\n", optopt);
                break;
        }
    }
    
    return 0;
}

# Примеры запуска:
gcc -o options options.c
./options -ips
./options -i -p -d
./options -x
________________________________________________________________
ИТОГОВОЕ Тестирование
# 1. Нет аргументов
./options

# 2. Опции разделены знаком минус (группировка)
./options -ips

# 3. Проверка порядка выполнения (справа налево)
./options -i -p -s

# 4. Повторение опций
./options -v -v

# 5. Изменение переменной окружения и проверка
./options -v -VTEST=hello | grep TEST

# 6. Неудачное значение для -U (отрицательное число)
./options -U-5

# 7. Изменение core file size
./options -C1024
./options -c 

____________________________________________

nano Makefile

Вставь это содержимое (важно: перед $(CC) и rm должен быть символ табуляции, не пробелы!):
CC = gcc
CFLAGS = -O2
TARGET = options
SRC = options.c

all: $(TARGET)

$(TARGET): $(SRC)
	$(CC) $(CFLAGS) -o $(TARGET) $(SRC)

clean:
	rm -f $(TARGET)

.PHONY: all clean
_____________________________________
# Разбор Makefile для сборки options.c

# === Переменные (упрощают изменение параметров) ===
CC = gcc          # компилятор
CFLAGS = -O2      # флаги оптимизации
TARGET = options  # имя выходного бинарника
SRC = options.c   # исходный файл

# === Цели (rules) ===

# all — цель по умолчанию (первая в файле)
# Зависит от $(TARGET), запускает сборку при вызове make без аргументов
all: $(TARGET)

# Правило сборки исполняемого файла
# $(TARGET) зависит от $(SRC): пересборка только если options.c новее options
$(TARGET): $(SRC)
	$(CC) $(CFLAGS) -o $(TARGET) $(SRC)
	# ↑ ТАБУЛЯЦИЯ! make требует tab перед командой, пробелы вызовут ошибку
	# Раскрывается в: gcc -O2 -o options options.c

# clean — удаление артефактов сборки
clean:
	rm -f $(TARGET)
	# ↑ ТАБУЛЯЦИЯ! -f подавляет ошибки если файл уже удалён

# .PHONY — объявляет цели как "фиктивные" (не соответствуют именам файлов)
# Без этого make может пропустить clean если существует файл с именем "clean"
.PHONY: all clean

# === Использование ===
make            # собрать (эквивалентно make all)
make clean      # удалить бинарник
make all        # явная сборка

______________________________________________________

Тестируем сборку через Makefile
# Очищаем старую сборку
make clean

# Собираем проект
make

# Проверяем работу
./options -i -p -d

Все 10 опций:
-i - выводит UID/GID
-s - делает процесс лидером группы
-p - выводит PID, PPID, PGID
-u - выводит ulimit (используем RLIMIT_NOFILE)
-U - изменяет ulimit
-c - выводит размер core-файла
-C - изменяет размер core-файла
-d - выводит текущую директорию
-v - выводит переменные окружения
-V - устанавливает переменную окружения
Порядок выполнения справа налево - опции сохраняются в массив и выполняются в обратном порядке
Обработка ошибок - невалидные опции и значения обрабатываются корректно
Makefile создан для удобства сборки

